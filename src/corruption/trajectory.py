import numpy as np
import torch


def build_trajectory(opt, log):
    log.info(f"[Trajectory] Building the image interpolations...")

    a = a.to(opt.device)

    def interpolate_image_array(img_arr, t_arr):
        return img_arr

    return interpolate_image_array
